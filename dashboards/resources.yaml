# Show Network frontend is shipped as one module to reduce Lovelace resource
# loading and keep all custom elements on one versioned asset.
- url: /api/dmx_monitor/static/show-network.js?v=0.11.9
  type: module
